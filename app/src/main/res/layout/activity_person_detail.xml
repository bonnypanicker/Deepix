<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/metroBgPrimary">

    <FrameLayout
        android:id="@+id/topBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="14dp"
        android:paddingBottom="6dp">

        <ImageButton
            android:id="@+id/backBtn"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="start|center_vertical"
            android:layout_marginStart="6dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/back"
            android:padding="12dp"
            android:src="@drawable/ic_fluent_back_24_regular"
            android:tint="@color/metroTextPrimary" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:fontFamily="sans-serif-light"
            android:includeFontPadding="false"
            android:text="@string/people_title_singular"
            android:textColor="@color/metroTextPrimary"
            android:textSize="20sp" />
    </FrameLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbars="none">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="20dp"
            android:paddingEnd="20dp"
            android:paddingBottom="24dp">

            <TextView
                android:id="@+id/personNameLabel"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:fontFamily="sans-serif-light"
                android:textColor="@color/metroTextPrimary"
                android:textSize="32sp" />

            <TextView
                android:id="@+id/personCountLabel"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:textColor="@color/metroTextSecondary"
                android:textSize="13sp" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:orientation="horizontal">

                <TextView
                    android:id="@+id/btnRename"
                    style="@style/PeopleActionButton"
                    android:text="@string/action_rename" />

                <TextView
                    android:id="@+id/btnMerge"
                    style="@style/PeopleActionButton"
                    android:text="@string/action_merge" />

                <TextView
                    android:id="@+id/btnHide"
                    style="@style/PeopleActionButton"
                    android:text="@string/action_hide" />

                <TextView
                    android:id="@+id/btnSplitLearnt"
                    style="@style/PeopleActionButton"
                    android:text="@string/action_split" />

                <TextView
                    android:id="@+id/btnUndo"
                    style="@style/PeopleActionButton"
                    android:text="@string/action_undo" />
            </LinearLayout>

            <TextView
                style="@style/SettingsSectionHeader"
                android:paddingTop="18dp"
                android:text="@string/faces_section" />

            <TextView
                android:id="@+id/facesCount"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="6dp"
                android:textColor="@color/metroTextSecondary"
                android:textSize="11sp" />

            <!-- Faces section: rendered as 3-column LinearLayout rows -->
            <LinearLayout
                android:id="@+id/facesContainer"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical" />
        </LinearLayout>
    </ScrollView>
</LinearLayout>
