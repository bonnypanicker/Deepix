<?xml version="1.0" encoding="utf-8"?>
<!-- Empty-state section: "Search by person" header + horizontal row of circular face thumbs. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="16dp"
    android:paddingTop="22dp"
    android:paddingEnd="0dp"
    android:paddingBottom="0dp">

    <TextView
        android:id="@+id/peopleHeader"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="16dp"
        android:text="@string/search_empty_people_header"
        android:textAppearance="@style/TextAppearance.Metro.SectionLabel" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/peopleRowList"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:clipToPadding="false"
        android:nestedScrollingEnabled="false"
        android:overScrollMode="never"
        android:paddingEnd="16dp" />
</LinearLayout>
